import Layout from '@/layouts/app';
import React from 'react';

const HelloWorld = () => {
    return (
        <Layout>
            <h1>Hello, World!</h1>
        </Layout>
    );
};

export default HelloWorld;